#!/usr/bin/python3
# this script defines how input symbols are mapped to numerical values

import numpy as np
import sys

def tagged_sents2feats(sents):
    """
    Given a list of sentences, return a list of integers
        Inputs:
            - sents: list of input sentences
            - word2idx: dictionary mapping words to embedding vectors
			- max_length: maximum allowed sentence length 
		Outputs:
			- X, Y
    """
    X = []
    y = []
    return X, y

